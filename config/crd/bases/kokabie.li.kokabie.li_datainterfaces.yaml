---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: datainterfaces.kokabie.li.kokabie.li
spec:
  group: kokabie.li.kokabie.li
  names:
    kind: DataInterface
    listKind: DataInterfaceList
    plural: datainterfaces
    singular: datainterface
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataInterface is the Schema for the datainterfaces API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DataInterfaceSpec defines the desired state of DataInterface
            properties:
              description:
                description: Description is a human-readable description of the data
                  interface
                type: string
              name:
                description: Name is the displayed name of the data interface
                type: string
              reference:
                description: Reference is a cluster-wide unique identifier for the
                  data interface if empty, the name will be used as reference instead
                type: string
              type:
                description: Type is the type of the data interface (e.g. "topic",
                  "queue", "database", "file")
                type: string
            type: object
          status:
            description: DataInterfaceStatus defines the observed state of DataInterface
            properties:
              usedInDataProcesses:
                description: UsedInDataProcesses is a list of data processes that
                  use this data interface
                items:
                  description: OwnerReference contains enough information to let you
                    identify an owning object. An owning object must be in the same
                    namespace as the dependent, or be cluster-scoped, so there is
                    no namespace field.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    blockOwnerDeletion:
                      description: If true, AND if the owner has the "foregroundDeletion"
                        finalizer, then the owner cannot be deleted from the key-value
                        store until this reference is removed. See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
                        for how the garbage collector interacts with this field and
                        enforces the foreground deletion. Defaults to false. To set
                        this field, a user needs "delete" permission of the owner,
                        otherwise 422 (Unprocessable Entity) will be returned.
                      type: boolean
                    controller:
                      description: If true, this reference points to the managing
                        controller.
                      type: boolean
                    kind:
                      description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                      type: string
                    name:
                      description: 'Name of the referent. More info: http://kubernetes.io/docs/user-guide/identifiers#names'
                      type: string
                    uid:
                      description: 'UID of the referent. More info: http://kubernetes.io/docs/user-guide/identifiers#uids'
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  - uid
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              usedReference:
                description: UsedReferences is the generated name of the data interface
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
